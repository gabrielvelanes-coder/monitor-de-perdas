.cache/
__pycache__/
*.pyc
_smoke.log
_run.log
# dados brutos (grandes / sensíveis) — ficam só na máquina
DADOS*.xlsx
BASE CADASTRO*.xlsx
perdas*.xls
perdas*.xlsx
*Baixa*Estoque*.xls*
faturamento.csv
faturamento.xlsx
faturamento.json
*itens*a*vencer*.xls*
*.parquet
# de-para loja -> regional (rotativo — editado à mão, não versionado)
regionais.csv

# base de custo "sugestão de compra" (ERP, ~88MB nos 3 arquivos) — fonte de
# custo mais confiável que o Custo Médio do DADOS, ver core.load_base_suges
base suges*.xlsx


!.streamlit/config.toml
streamlit.log
